{
  "name": "@imap-mcp/sync",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node ../../scripts/deploy-config.mjs sync && wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit",
    "build": "wrangler deploy --dry-run",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@imap-mcp/imap": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^26.2.0",
    "@vitest/coverage-istanbul": "^4.1.9",
    "typescript": "^7.0.2",
    "vitest": "^4.1.8",
    "wrangler": "^4.105.0"
  }
}
