{
  "name": "@imap-mcp/imap",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Internal IMAP interface. Wraps cf-imap so the rest of the repo never touches it directly.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "cf-imap": "1.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^4.20250109.0",
    "@vitest/coverage-istanbul": "^4.1.9",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
