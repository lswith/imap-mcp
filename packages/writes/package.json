{
  "name": "@imap-mcp/writes",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "The write contract both workers are written against. Types only: it imports nothing, so it can sit between a worker that holds the mailbox credential and one that must never see it.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
